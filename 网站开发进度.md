# 公共知识站开发进度

更新时间：2026-08-13

## 问题索引

- Q1：当前网页化和公开发布准备完成到什么程度？
- Q2：哪些历史笔记已确认属于私有来源，如何处理？
- Q3：后续按什么阶段继续开发和上线？

## Q1：当前状态

### 已完成

- [x] 确认本机 Node.js 为 `v24.19.0`，满足 VuePress 运行要求。
- [x] 选择 VuePress 2、Vite 和 `vuepress-theme-hope` 作为首期技术基线。
- [x] 建立 GitHub Pages Actions 工作流、站点导航、自动侧边栏和本地搜索配置。
- [x] 建立 Git 忽略、页面排除和源码/产物扫描三层发布边界。
- [x] 清理一级目录 README、根目录导航和系统设计覆盖矩阵中的私有链接。
- [x] 将公共发布规则沉淀到 [发布与脱敏规则.md](发布与脱敏规则.md)。

### 当前待验证

- [x] 生成依赖锁文件并完成 VuePress 首次构建。
- [x] 通过源码扫描、构建产物扫描和页面路由检查。
- [x] 完成新 Git 仓库初始化，提交身份使用通用 no-reply 信息，并清理不可达旧对象。
- [x] 增加可达 Git 历史扫描，防止删除当前文件后遗漏历史内容。
- [x] 顶部导航改为一级目录直出，并按二级目录分组展示全部公开文章。
- [x] 侧栏改为非排他展开，增强目录与文章的视觉区分。
- [x] 增加会话级已打开页面标签栏，可切换、关闭和清空。
- [x] 统一主分支为 `master`，删除本地 `main`，并将 GitHub Pages 工作流触发分支同步为 `master`。
- [x] 删除两个旧 Git 历史备份目录，当前公开仓库只保留新的 `master` 提交链。
- [x] 创建 GitHub 远程仓库并推送新的 `master` 提交链。
- [ ] 在 GitHub 仓库 Settings -> Pages 中将发布源设为 GitHub Actions，然后重新运行发布工作流。

### 本轮阅读体验实现

- 顶部导航不再按主题自动聚合。九个公开一级目录直接作为导航项；下拉菜单按其二级目录分组，目录标题加粗，并在组间显示分割线。
- 导航文章清单只读取公开文章。页面构建和导航共用 `.vuepress/private-content.ts` 的过滤规则，本地私有资料不会生成菜单链接。
- 侧栏使用独立展开状态集合，展开一个目录不会关闭其他目录；文件夹采用加粗文字和文件夹图形，文章使用较小的文档图形。
- 已打开页面栏使用 VuePress 客户端扩展实现，使用浏览器 `sessionStorage` 保存本次会话的最近 10 个页面，支持切换、关闭和清空。未引入兼容性不明确的第三方页签插件。
- 已在 1440px 桌面和 390px 移动宽度截图检查一级导航、侧栏层级和页签布局；当前公开文章共 69 篇，目录深度均不超过二级。

### 本轮导航与阅读体验修复（2026-08-13）

- [x] 侧栏和顶部导航改用数字自然排序，保持本地编号顺序；同级目录排在文档前，展示文本隐藏目录数字前缀。
- [x] 顶部导航下拉层改为相对一级导航项左对齐。
- [x] 已打开页面页签固定在导航栏下方的右上角，并改为逐行纵向排列，长列表在页签区域内滚动。
- [x] 页面大纲只收集 `Q1`、`Q2` 等问题标题；桌面端默认收起，点击大纲标题负责定位，点击大纲标题栏负责展开或收起。
- [x] PlantUML 配置兼容现有的 `plantuml`、`puml` 和 `uml` 围栏写法，构建产物已生成 PlantUML 图片而不是代码块。
- [x] `npm.cmd run docs:build` 成功渲染 119 个页面；产物包含 69 个 PlantUML 图片页面，未检出未渲染的 PlantUML 围栏代码块。

## Q2：历史私有来源笔记清单

已确认 13 篇笔记带有明确的历史私有来源标记。文件保留在本地，已从 Git 跟踪、VuePress 页面和目录导航中排除；完整路径清单保存在被忽略的本地审计文件 `私有资料审计清单.md`，不会进入公共站点或新 Git 历史。

### 公开联动检查结果

- 根目录导航和目录说明中的私有入口已移除。
- 各一级目录及 ORM 子目录中指向上述文件的链接已移除。
- 系统设计覆盖分析已按公开笔记重新计算，未再把私有资料作为覆盖依据。
- 公开内容仍可保留通用术语，例如 CRM、订单、缓存或消息队列；这些术语不等同于个人资料。扫描规则针对可识别名称、私有来源标记和私有路径。

## Q3：后续阶段

| 阶段 | 目标 | 交付物 | 验收条件 | 状态 |
| --- | --- | --- | --- | --- |
| P0 | 建立可公开的站点基础 | VuePress、主题、工作流、三层防护 | `npm run docs:verify` 通过 | 已完成 |
| P1 | 初始化远程发布 | 新 GitHub 仓库与 Pages 设置 | 启用 Actions Pages 后成功发布 | 进行中 |
| P2 | 验证阅读体验 | 中文搜索、PlantUML、移动端侧栏、下拉导航和会话页签检查 | 选取代表性笔记逐项通过 | 进行中 |
| P3 | 整理公开内容 | 补齐缺失专题和目录入口 | 新笔记满足公开规则并通过扫描 | 待开始 |
| P4 | 持续维护 | 每次新增内容执行发布检查 | PR 或本地提交前检查留痕 | 待开始 |

### P1 执行清单

1. 已在 GitHub 创建新的空仓库，未导入旧仓库历史。
2. 已将本地新 `master` 分支添加为该仓库远程并首次推送。
3. 在仓库 Settings -> Pages 中将发布源设置为 GitHub Actions。
4. 重新运行 `Deploy public documentation to GitHub Pages` 工作流，记录发布 URL。

### P1 首次发布故障记录

- 现象：`Configure GitHub Pages` 返回 `Get Pages site failed: Not Found`，构建任务在依赖安装前终止。
- 原因：仓库尚未启用 GitHub Pages，`actions/configure-pages` 无法读取 Pages 站点配置；这不是 VuePress 构建或站点内容错误。
- 处理：工作流升级为 `actions/configure-pages@v6`，消除其 Node.js 20 弃用警告。仍需由仓库管理员在 Settings -> Pages 中把发布源设为 GitHub Actions；默认 `GITHUB_TOKEN` 无权自动完成该仓库级启用动作。
- 验收：重新运行后，`Configure GitHub Pages` 成功，随后依次通过公开边界检查、构建、产物检查和 Deploy 任务。

### P2 验收样本

- 至少检查一篇含 PlantUML 的架构笔记、一篇含 Java 代码的笔记、一篇长表格笔记和一篇中文标题较长的笔记。
- 分别在桌面和移动宽度下检查下拉导航、非排他侧边栏、会话页签、目录、代码块和搜索结果。
- 使用 [发布与脱敏规则.md](发布与脱敏规则.md) 中的命令复查公开边界。

### P2 本轮验收记录

- [x] 桌面宽度下，一级目录导航可完整显示，架构设计侧栏能同时展开多个目录。
- [x] 移动宽度下，固定页签按右上角纵向排列，并限制在导航栏下方的可视区域内。
- [x] 构建产物包含分布式系统理论分组和 Snowflake 雪花 ID 文章路由，且公开边界扫描通过。
- [ ] 完成中文搜索和包含 PlantUML、长表格、代码块的代表性文章交互验收。

## 复习清单

- [x] 能说明为什么本地私有资料不进入公开站点。
- [x] 能找到全部 13 篇历史私有来源笔记。
- [ ] 能完成首次 GitHub Pages 发布。
- [ ] 能完成移动端和中文搜索验收。
